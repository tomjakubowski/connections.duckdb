[project]
name = "connections-duckdb"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "duckdb>=1.4.4",
]

[dependency-groups]
dev = [
    "polars>=1.38.1",
    "pyarrow>=23.0.1",
    "pytest>=9.0.2",
]
